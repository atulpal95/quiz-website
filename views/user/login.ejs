<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Form</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background: linear-gradient(120deg, #bb8b8d, #fad0c4); /* Gradient background */
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }
        
        form {
            background: #ffffff;
            border-radius: 10px; 
            box-shadow: 0px 8px 15px rgba(0, 0, 0, 0.2); 
            padding: 2rem;
            width: 100%;
            max-width: 400px; 
        }
        
        form .form-label {
            font-weight: bold;
            color: #333; 
        }
        
        form .form-control {
            border: 2px solid #e3e3e3; 
            border-radius: 5px;
            padding: 0.5rem;
            font-size: 1rem;
            transition: border-color 0.3s;
        }
        
        form .form-control:focus {
            border-color: #ff758c; 
            outline: none;
            box-shadow: 0px 0px 8px rgba(255, 117, 140, 0.5); 
        }
        
        form button {
            background-color: #4c169d; 
            color: #fff; 
            border: none;
            border-radius: 5px;
            padding: 0.75rem 1.5rem;
            font-size: 1rem;
            font-weight: bold;
            cursor: pointer;
            transition: background-color 0.3s ease, transform 0.2s ease;
            width: 100%; 
        }
        
        form button:hover {
            background-color: #ff506e; 
            transform: scale(1.05); 
        }
        
        form button:active {
            background-color: #e8435f; 
            transform: scale(1); 
        }
        
        .mb-3 {
            margin-bottom: 1.5rem;
        }
        
        .signup-link {
            text-align: center;
            margin-top: 15px;
        }

        .signup-link a {
            color: #4c169d;
            font-weight: bold;
            text-decoration: none;
            transition: color 0.3s ease;
        }

        .signup-link a:hover {
            color: #ff506e;
        }

        @media (max-width: 768px) {
            form {
                width: 90%; 
                padding: 1.5rem;
            }
        }
    </style>
</head>
<body>

    <div class="row mt-3">
        <h1 class="col-6 offset-3">Login to Quiz</h1>
        <div class="col-6 offset-3">

            <form action="/login" method="post" class="needs-validation" novalidate>
                <div class="mb-3">
                    <label for="username" class="form-label">Username</label>
                    <input type="text" id="username" name="username" placeholder="Enter the username" class="form-control" required>
                </div>
                <div class="mb-3">
                    <label for="password" class="form-label">Password</label>
                    <input type="password" id="password" name="password" placeholder="Enter the password" class="form-control" required>
                </div>
                <button class="btn btn-success">Login</button>
            </form>

            <div class="signup-link">
                <p>Don't have an account? <a href="/signup">Sign Up</a></p>
            </div>

        </div>
    </div>  

</body>
</html>
